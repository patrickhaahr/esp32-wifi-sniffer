services:
  mosquitto:
    image: eclipse-mosquitto
    container_name: mosquitto
    ports:
      - "8883:8883"  # MQTT over TLS only
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - ./certs:/mosquitto/config/certs:ro  # TLS certificates (read-only)
    restart: unless-stopped
